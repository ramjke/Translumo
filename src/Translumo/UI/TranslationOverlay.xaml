<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="Translumo.UI.TranslationOverlay"
    Width="360" Height="120"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize">
  <Border Background="#CC222222" CornerRadius="6" Padding="10" >
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock x:Name="OriginalText" Foreground="White" FontSize="13" TextWrapping="Wrap" />
      <TextBlock x:Name="TranslatedText" Grid.Row="1" Foreground="#FFE9E9E9" FontSize="15" FontWeight="Bold" Margin="0,6,0,0" TextWrapping="Wrap" />

      <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
        <Button x:Name="PinButton" Content="Pin" Margin="0,0,6,0" Padding="6,2"/>
        <Button x:Name="AnkiButton" Content="Send to Anki" Margin="0,0,6,0" Padding="6,2"/>
        <Button x:Name="CloseButton" Content="✕" Padding="6,2"/>
      </StackPanel>
    </Grid>
  </Border>
</Window>
